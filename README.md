# Snapdrop
A lightweight Windows screenshot and screen-recording tool with tray controls, global shortcuts, floating previews, local history, annotation tools, and optional sharing.

Windows CI is enabled for build verification.

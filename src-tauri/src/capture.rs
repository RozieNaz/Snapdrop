pub struct ScreenshotService;

impl ScreenshotService {
    pub fn new() -> Self {
        Self
    }

    pub fn capture_fullscreen(&self) {
        // Windows capture implementation to be added.
    }
}
